import React from "react"
import * as Styled from "./styled"

const Terminal = () => {
  return (
    <Styled.TerminalWrapper>
      <Styled.TerminalMenu />
    </Styled.TerminalWrapper>
  )
}

export default Terminal
